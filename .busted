return {
  _all = {
    -- Test file patterns (matches test_*.lua and *_spec.lua)
    pattern = {"_spec", "test_"},

    -- Exclude test helper and old test runner
    exclude = {
      "test_helper.lua",
      "test_all.lua"
    },

    -- Root directories for tests
    ROOT = {"tests/", "spec/"},

    -- Lua path for source code
    lpath = "lib/?.lua;lib/?/init.lua",

    -- Verbose output
    verbose = false,

    -- Coverage (optional, requires luacov)
    coverage = false,
  },

  -- Default configuration
  default = {
    verbose = false,
    coverage = false,
  },

  -- Verbose mode
  verbose = {
    verbose = true,
  },

  -- Coverage mode
  coverage = {
    coverage = true,
  }
}
